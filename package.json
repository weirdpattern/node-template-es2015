{
  "version": "0.0.1",
  "license": "MIT",

  "name": "package.name",
  "description": "package.description",
  "author": "package.author",

  "main": "index.js",

  "engines": {
    "node": "^4.4.3"
  },

  "dependencies": {
    "babel-core": "^6.9.1",
    "babel-polyfill": "^6.9.1",
    "babel-preset-es2015": "^6.9.0"
  },

  "devDependencies": {
    "snazzy": "*",
    "semistandard": "*",
    "babel-eslint": "*",
    "tape": "*",
    "tap-summary": "*",
    "babel-istanbul": "*"
  },

  "scripts": {
    "lint": "semistandard --verbose | snazzy",
    "specs": "node ./spec/index.js | tap-summary",
    "test": "npm run lint && npm run specs",
    "coverage": "babel-istanbul cover ./spec/**/*.js",
    "review": "npm run test && npm run coverage"
  },

  "semistandard": {
    "parser": "babel-eslint"
  }
}